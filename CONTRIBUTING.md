# Contributing

Run `npm run lint` and `npm run build` before opening a pull request. Keep UI presentational, locate content transformation in entity mappers, and introduce CMS-specific behavior only in adapters. New features should be composed from existing shared primitives before adding a new one.
