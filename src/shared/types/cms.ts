export type CmsArticleDto = { id: string; slug: string; title: string; excerpt?: string; content?: string; featuredImage?: { url: string; alt?: string; caption?: string }; categories: Array<{ name: string; slug: string }>; tags: Array<{ name: string; slug: string }>; author: { name: string; slug: string; description?: string; avatarUrl?: string }; date: string; modified?: string; readingTime?: number };
export interface CmsAdapter { getArticle(slug: string): Promise<CmsArticleDto | null>; listArticles(input?: { category?: string; author?: string; query?: string; cursor?: string }): Promise<{ nodes: CmsArticleDto[]; nextCursor?: string }> }
